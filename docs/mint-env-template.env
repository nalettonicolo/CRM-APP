# Usa invece il file COMPLETO (non template):
# backend/.env.mint-RIPRISTINO.txt
#
# Su Mint: nano ~/CRM-APP/backend/.env
# Incolla tutto il contenuto di .env.mint-RIPRISTINO.txt
# Modifica SOLO JWT_* se sul server ne avevi di diversi da openssl.
