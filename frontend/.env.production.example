# Variabili da impostare su Netlify (Environment variables).
# Non serve file .env in repo per produzione se compili solo su Netlify.

NEXT_PUBLIC_API_URL=https://api.tuo-dominio.it
NEXT_PUBLIC_APP_NAME=Nicolò Service
